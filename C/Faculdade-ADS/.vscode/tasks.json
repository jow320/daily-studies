{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Compilar C",
      "command": "gcc",
      "args": [
        "-g",
        "-fexec-charset=UTF-8",
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}.exe"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
