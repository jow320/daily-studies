#include <stdio.h>

int main (){

//Incremento (++), pré(++a), pós(a++)
//Decremento (--), pré(--a), pós(a--)

int numero1 = 1, resultado;

printf("Antes do incremento: %d\n", numero1);

numero1++;

printf("Após do incremento: %d\n", numero1);
}

