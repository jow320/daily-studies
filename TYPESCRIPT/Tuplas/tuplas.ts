let point: [number, number, string, boolean];

//Anotações relevantes

point = [2, 5, "Test", true];

// Definição de uma tupla
let person: [string, number];

// Inicializando a tupla com valores
person = ["João", 25];

// Acessando os valores
console.log(person[0]); // João (string)
console.log(person[1]); // 25 (number)
